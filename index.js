//initialize the 3d scene and AR code
init();

//load the model: the first parameter is the url to the model, the second is the scale of the model (it works only with gltf filese)
loadModel('./busterDrone/shere.gltf',1);
//loadModel('https://raw.githubusercontent.com/u4bi-git/arjs-3dmodel-loader-test/master/assets/model/knight/knight.js');

